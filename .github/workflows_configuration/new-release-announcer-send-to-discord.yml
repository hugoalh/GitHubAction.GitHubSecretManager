# content:
# username:
# avatar_url:
tts: false
embeds:
  - title: "[GitHub Action] GitHub Secret Manager"
    description: "Version %payload.release.name% (%payload.release.tag_name%) is released and available on:"
    # url:
    color: "16,116,231"
    # footer:
      # text:
      # icon_url:
    # image:
      # url:
    thumbnail:
      url: "https://i.imgur.com/knmLbFg.png"
    # video:
      # url:
    # author:
      # name:
      # url:
      # icon_url:
    fields:
      - name: "GitHub Repository Release (& Changelog)"
        value: "%payload.release.html_url%"
        inline: false
      - name: "GitHub Marketplace"
        value: "https://github.com/marketplace/actions/github-secret-manager"
        inline: false
      - name: "GitHub Repository"
        value: "https://github.com/hugoalh/GitHubAction.GitHubSecretManager"
        inline: false
